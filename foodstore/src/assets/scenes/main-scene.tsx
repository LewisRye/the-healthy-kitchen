import { useState } from "react";


export const MainScene = () => {
  const [scene, setScene] = useState("main");

  return (
    <>
      {scene === "main" && (
        <>
          
        </>
      )}
    </>
  );
};